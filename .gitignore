envs/
__pycache__/
*.py[cod]
.vscode/

# Build artifacts — regenerated locally by build_release.ps1 / PyInstaller.
build/
dist/
release/
dist-private/
research_private/

# Local detector test images (downloaded datasets must not enter releases).
test_images/

# Runtime logs / user data (the app writes these under %LOCALAPPDATA%\OnlyMyFace).
*.log
only_my_face_settings.json
only_my_face_exemptions.json

# Secrets / credentials — nothing here today, but never let these get committed.
.env
*.env
*.key
*.pem
*.pfx
secrets.*
credentials.*
*_token*
*.local.json
